# Lockdown hook

`.claude/lockdown-deny.sh` is auto-generated by `coily lockdown --apply`. It delegates to `coily hook pre-tool-use`, which calls the shared cli-guard hook engine with coily's integrity rules and routing table.

Background: coilysiren/coily#248, cli-guard#74. The cross-consumer reference (previously `exec agent-guard hook pre-tool-use`) is gone. coily and agent-guard are peer cli-guard consumers that do not name each other in source.

Regenerate with `coily lockdown --apply --replace`.

Note: coily's current `coilyRenderHookScript` template (cmd/coily/lockdown_driver.go) emits a 5-line contiguous comment block that exceeds the agentic-os `code-comments` hook's MAX_CONTIGUOUS=2 cap. This file's hand-trimmed header will be regenerated to the long form on the next `coily lockdown --apply`. Upstream fix tracked separately.
